{
  "name": "decant",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p packages/schemas/tsconfig.json --noEmit && tsc -p packages/core/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
